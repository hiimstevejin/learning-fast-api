# FASTAPI

## How to run server
In terminal
<uviconr> <name of file> <name of app> --reload(auto refresh)
```bash
uvicorn main:app --reload
```
## Documentation
In localhost
/docs
takes you to a UI that shows list of endpoints
